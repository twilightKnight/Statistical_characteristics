from setuptools import find_packages, setup

setup(
    name='statistical_characteristics',
    packages=find_packages(include=['statistical_characteristics']),
    version='0.0.1',
    description='My first Python library',
    author='Me',
    install_requires=['numpy'],
    license='MIT',
)
